/*GestureDetector(
                  onTap: () {
                    setState(() {
                      toqueii = !toqueii;
                    });
                  },
                  child: AnimatedContainer(
                    decoration: BoxDecoration(
                        borderRadius: BorderRadius.circular(toqueii ? 20 : 0),
                        color: !toqueii ? Colors.red : Colors.amber[900]),
                    duration: Duration(milliseconds: 800),
                    curve: Curves.bounceOut,
                    height: !toqueii ? altura = 300 : altura = 180,
                    width: !toqueii ? largura = 300 : largura = 180,
                    child: Text(toqueii ? 'Não Mudei' : 'Mudei',
                        style: GoogleFonts.fredoka(
                            textStyle: const TextStyle(
                                fontWeight: FontWeight.w600,
                                fontSize: 25,
                                color: Colors.white))),
                  ),
                ),
*/